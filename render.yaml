services:
  - type: web
    name: ai-architect
    runtime: python
    buildCommand: pip install .
    startCommand: PYTHONPATH=src uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PORT
        generateValue: false
